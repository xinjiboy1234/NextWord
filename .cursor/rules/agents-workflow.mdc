---
description: NextWord Agent 工作规范（必须遵守）
alwaysApply: true
---

# Agent 工作规范

本仓库内所有 AI Agent / coding assistant **必须遵守** [`.codex/AGENTS.md`](../../.codex/AGENTS.md) 中的完整规范。以下为关键摘要，冲突时以 AGENTS.md 为准。

## 开发流程

- **开发前**：输出需求理解、影响范围、实现计划、风险点、验收标准；未经计划确认不直接编码
- **开发中**：每完成一个阶段执行本地 Git Commit、更新 `development-log.md` 与 `next-steps.md`
- **开发结束**：输出已完成/未完成、已知问题、下一步、涉及文件、是否建议提交

## 设计与修改

- 禁止过度设计（提前抽象/插件化/微服务化）与极简设计（逻辑堆叠、缺少分层）
- 优先遵循现有目录结构、命名、架构风格
- **最小修改**：仅改需求相关代码，禁止顺手重构或格式化无关文件
- 数据库变更须提供 Migration、Upgrade Script、Rollback Script
- 实体/DbContext 变更后见 [ef-migrations.mdc](./ef-migrations.mdc)：先测 DB 连接，再 `ef-migrate.ps1` 迁移

## 测试与文档

- 新功能须提供单元测试或说明无法编写的原因
- Bug 修复须补充复现与验证测试
- 持续维护 `development-log.md` 与 `next-steps.md`

## 任务结束输出格式

```text
本次完成 / 修改文件 / 风险 / 下一步 / 是否建议提交 (YES/NO)
```
